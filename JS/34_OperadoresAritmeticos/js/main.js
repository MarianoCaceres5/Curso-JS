// Operadores

// operando operador (unarios)
// operando1 operador operando2 (binarios)
// operando ternario


//OPERADORES ARITMETICOS

// + y -

let resultado = 6;
let factorCorreccion = 2;

let puntuacion = resultado - factorCorreccion;

// El operador suma en el caso de tipos string los concatena

let nombre = 'Laura';
let mensaje = 'Hola ';

mensaje = mensaje + nombre;

//
resultado = 10;
factorCorreccion = '3';
puntuacion = resultado + factorCorreccion;


// * y /

let a = 10;
let b = 2;
resultado = a / b;

// Valor NaN (not a number)

a = 'Hello';
resultado = a*b;

// Resto %

let ejemploResto = 13 % 2;

// Incremento ++ y decremento --

let c = 1;
c++;

// Precedencia de operadores
// de derecha a izquierda y el mismo criterio que matematicas

resultado = 12 + 3*8

// Podemos usar parentesis para romperar la precedencia de operadores